# SkinsCaddy API (FastAPI)
# Install: pip install -r requirements.txt

# Web
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.12

# Database (Postgres on Render, SQLite locally)
sqlalchemy>=2.0.36
psycopg[binary]>=3.2.0

# Settings / env
pydantic>=2.10.0
pydantic-settings>=2.6.0
python-dotenv>=1.0.1

# Auth
PyJWT>=2.10.0
bcrypt>=4.2.0
# RS256 for FCM HTTP v1 (optional if only FCM_SERVER_KEY is set)
cryptography>=43.0.0
